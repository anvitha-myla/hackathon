# Residual ML artifacts (joblib). Generated demo models may be committed for CI.
# Cold-start: if this directory has no *.joblib, ResidualCorrectionEngine
# returns δ_ML = 0 with is_pure_physics=True.
